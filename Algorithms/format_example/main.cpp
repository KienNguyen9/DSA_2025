#include <iostream>
#include <fstream>
#include <sstream>
#include <string>

#define MAX 100

int k, n;           // Gía trị cho trước
int arr[MAX];       // Mảng a
int len;            // Số phần tử sẽ được sử dụng

int main()
{
    
    return 0;
}